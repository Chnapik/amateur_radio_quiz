import 'package:flutter/material.dart';

void main() {
  runApp(const AmateurRadioQuiz());
}

class AmateurRadioQuiz extends StatelessWidget {
  const AmateurRadioQuiz({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
